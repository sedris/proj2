<h1>Listing Users</h1>
<table>
  <tr>
    <th>Email</th>
    <th>Type</th>
    <th></th>
    <th></th>
  </tr>

<% @users.each do |user| %>
  <tr>
    <td><%= user.email %></td>
    <td><%= user.type %></td>
    <td><%= link_to 'Show', user %></td>
    <td><%= link_to 'Destroy', user, method: :delete, data: { confirm: 'Are you sure?' } %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Shopper', new_shopper_path %> <br>
<%= link_to 'New Shopkeeper', new_shopkeeper_path %>